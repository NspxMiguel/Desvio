# A Groq key can live here instead of in the app's Settings screen.
GROQ_API_KEY=gsk_your_key_here

# Force the interface language for one run: pt or en. Leave unset to follow the
# system, or whatever was chosen in Settings.
DESVIO_LANG=
